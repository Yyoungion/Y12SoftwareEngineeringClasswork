def check_api_key(key):
    keys = ["key1", "key2", "mysecret"]
    if key in key:
        return True
    else:
        return False

# Modify so it accepts any key from: ["key1", "key2", "mysecret"]
print(check_api_key("wrongkey"))  # False